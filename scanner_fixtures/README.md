Synthetic board responses used to test the filter offline.

These are NOT real postings. They copy the JSON shape of each API so
`python job_scanner.py --dry-run --fixtures scanner_fixtures` exercises the
Greenhouse, Lever, and Ashby parsers and the location and title filter
without touching the network. File names are `<board>_<slug>.json`.
